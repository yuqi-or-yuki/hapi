// Code Mode dynamically exposes every non-hidden executor. Match the same open
// tool set here; the bridge filters direct calls by their non-exec call ids.
export const CODEX_TOOL_HOOK_MATCHER = '*';
